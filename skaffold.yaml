apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/stevef1uk/goproxy:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/stevef1uk/goproxy:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: goproxy
        chartPath: charts/goproxy
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/stevef1uk/goproxy"
          image.tag: "{{.DIGEST_HEX}}"
